buildscript {
    apply from: 'scripts/dependencies.gradle'

    repositories {
        maven {
            url "https://plugins.gradle.org/m2/"
        }
    }
    dependencies {
        classpath gradlePlugins.apt
    }
}

allprojects {
    repositories {
        mavenCentral()
    }
}